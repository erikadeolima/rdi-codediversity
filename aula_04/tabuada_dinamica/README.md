# Projeto C# - Console Application

## 📋 PASSO A PASSO PARA INICIALIZAR E RODAR O PROJETO

### 1. Verificar se o .NET está instalado
```bash
dotnet --version
```
Se aparecer uma versão (ex: 8.0.x), está instalado corretamente.

### 2. Entrar no diretório do projeto
```bash
cd aula_04
```

### 3. Rodar o projeto
```bash
dotnet run
```

### 4. Para adicionar novos algoritmos
Edite o arquivo `Program.cs` e adicione seu código dentro do método `Main`.

### 5. Para criar um novo projeto Console Application (se precisar no futuro)
```bash
dotnet new console -n NomeDoProjeto -o nome_da_pasta
```

---

## 📁 Estrutura do Projeto
```
aula_04/
├── Program.cs          # Código principal (seu algoritmo)
└── Aula04App.csproj    # Arquivo de configuração do projeto
```

---

## ✅ Comandos Úteis
| Comando | Descrição |
|---------|-----------|
| `dotnet run` | Compila e executa o projeto |
| `dotnet build` | Apenas compila o projeto |
| `dotnet clean` | Limpa arquivos compilados |
| `dotnet restore` | Restaura dependências |

---

## 🚀 Executando o projeto
No terminal, execute:
```bash
cd aula_04 && dotnet run
```

